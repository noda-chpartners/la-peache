// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
	// OGP・canonical に使用。本番ドメインに差し替えてください
	site: 'https://la-peache.pages.dev',
});
